<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Du vrac entassé | vbesse.com</title>
	</head>
	<body>
		<header>
			<nav>
				<a href="/" class="site-title">vbesse.com</a>

				<div class="nav-links">
					<a href="/">Accueil</a>
					<a href="/blog">Blog</a>
					<a href="/about">À propos</a>
				</div>
			</nav>
		</header>
		<main>
			<slot />
		</main>
		<footer></footer>
	</body>
</html>

<style is:global>
	html,
	:root {
		/* Une palette élégante : Fond blanc cassé, texte anthracite, bleu électrique pour les liens */
		--bg-color: #ffffff;
		--text-color: #1a1a1a;
		--accent-color: #0070f3;
		--gray-light: #f4f4f4;
		--accent: #232946; /* Une couleur sombre pour le texte */
		--accent-light: #eebbc3; /* Une couleur douce pour les fonds */
		--accent-dark: #b8c1ec; /* Une couleur pour les boutons */
		--brand-primary: #0070f3; /* Une couleur vive pour les accents et les liens */
	}

	body {
		background-color: var(--bg-color);
		color: var(--text-color);
		font-family:
			"Inter Variable",
			-apple-system,
			BlinkMacSystemFont,
			"Segoe UI",
			Roboto,
			Oxygen,
			Ubuntu,
			Cantarell,
			"Open Sans",
			"Helvetica Neue",
			sans-serif;
		line-height: 1.6;
		margin: 0;
		padding: 0;
		-webkit-font-smoothing: antialiased;
		-moz-osx-font-smoothing: grayscale;
	}

	main {
		max-width: 720px; /* Largeur idéale pour la lecture */
		margin: 0 auto; /* Centre le bloc horizontalement */
		padding: 2rem 1rem; /* Ajoute une marge interne pour ne pas toucher les bords sur mobile */
	}

	a {
		color: var(--accent-color);
		text-decoration: none;
	}

	a:hover {
		text-decoration: underline;
	}

	header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05); /* Une ombre très légère pour détacher le menu du contenu */
		position: sticky; /* (Optionnel) Le menu reste accroché en haut quand on scrolle */
		top: 0;
		z-index: 10;
	}

	nav {
		display: flex;
		align-items: center;
		justify-content: space-between; /* C'est LA commande magique qui pousse les éléments aux extrémités */
		max-width: 1000px; /* On autorise le menu à être plus large que le texte des articles */
		margin: 0 auto;
		padding: 1rem 0;
	}

	.site-title {
		font-size: 1.3rem;
		font-weight: 800; /* Très gras pour ressortir comme un logo */
		color: var(--text-color);
		text-decoration: none;
		letter-spacing: -0.5px;
	}

	.site-title:hover {
		text-decoration: none; /* Pas de soulignement sur le logo */
	}

	.nav-links {
		display: flex;
		gap: 1.5rem; /* Espace de 24px entre chaque lien */
	}

	.nav-links a {
		color: #555; /* Gris foncé, plus élégant que le noir pur */
		text-decoration: none;
		font-weight: 500;
		font-size: 1rem;
		transition: color 0.2s ease;
	}

	.nav-links a:hover {
		color: var(--accent-color); /* Tes liens deviennent bleus au survol */
	}
</style>
